---
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const H: any = `h${Astro.props.headingLevel}`

type Props = {
    headingLevel: 1 | 2 | 3 | 4 | 5 | 6
    class?: string
}
---

<H class={Astro.props.class}>
    <slot />
</H>
